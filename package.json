{
  "name": "random-cattos",
  "version": "1.0.0",
  "scripts": {
    "dev": "rimraf dist && parcel ./src/index.html",
    "build": "rimraf dist && parcel build ./src/index.html --no-source-maps"
  },
  "author": "elhlab",
  "license": "MIT",
  "description": "A very useless website for generating random HTTP status cats.",
  "devDependencies": {
    "parcel": "^2.16.4",
    "rimraf": "^6.1.3"
  },
  "dependencies": {
    "statuses": "^2.0.2"
  }
}
